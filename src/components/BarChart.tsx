import { FC } from "react";
import { IMonthlyApplications } from "../types/IStats";

const BarChart: FC<{ data: IMonthlyApplications[] }> = ({ data }) => {
  return <h2>BarChart</h2>;
};

export default BarChart;
